// app/components/Header.jsx
"use client"

import NextSnowScreen from "../components/snow/NextSnowScreen"
import ThemeToggle from "../components/ThemeToggle"

export default function Header() {
  return (
    <header className="sticky top-0 z-50 bg-slate-100 dark:bg-slate-900 border-b border-slate-200 dark:border-slate-700">
      <div className="mx-auto max-w-[1100px] px-4">
        <div className="flex h-16 items-center justify-between
                        max-md:h-auto max-md:flex-col max-md:gap-2 max-md:py-3">

          {/* Logo */}
          <a href="#" className="text-lg font-bold text-slate-800 dark:text-slate-100">
            Hibike!
            <span className="text-indigo-600"> Euphonium</span>
          </a>

          {/* Nav */}
          <nav className="flex gap-6 max-sm:flex-wrap max-sm:justify-center">
            {["Episodes", "Characters", "About"].map((item) => (
              <a
                key={item}
                href={`#${item.toLowerCase()}`}
                className="relative text-sm font-medium
                           text-slate-700 dark:text-slate-200
                           after:absolute after:left-0 after:-bottom-1
                           after:h-[2px] after:w-0 after:bg-indigo-600
                           after:transition-all hover:after:w-full"
              >
                {item}
              </a>
            ))}
          </nav>

          {/* Right Actions */}
          <div className="flex items-center gap-3">
            <NextSnowScreen />
            <ThemeToggle />
          </div>
        </div>
      </div>
    </header>
  )
}
